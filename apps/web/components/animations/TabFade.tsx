/**
 * TabFade - Animation utility for smooth text color transitions
 * 
 * Provides 300ms ease-out transition for navbar tab color changes.
 * Only the active tab fades in; inactive tabs remain static.
 */

export const tabFadeTransition = "transition-colors duration-300 ease-out";

